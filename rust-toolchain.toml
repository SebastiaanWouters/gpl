[toolchain]
channel = "1.85.0"
profile = "minimal"
components = ["rustfmt", "clippy"]

# This file pins the currently supported development toolchain for GPL v1 work.
# It is not yet an MSRV promise; upgrade intentionally with matching doc/CI updates.
